# Tasks: liquid-glass-ui-kit

## Phase 0: Foundation (P0 — must be done first, unblocks everything)

- [ ] TASK-001: Strip duplicate styles from app.scss — `src/app/app.scss` — Remove `.liquid-glass-card`, `.liquid-glass-button`, `.liquid-glass-button-outline`, `.text-glow`, `.text-glow-teal`, `.no-scrollbar`, `@keyframes float`, and `.animate-float`; leave only `:host { display: block; position: relative; width: 100%; height: 100%; }` and the `@tailwind` directives.
- [ ] TASK-002: Add missing radius tokens to styles.scss — `src/styles.scss` — Insert `--lg-radius-card: 2rem`, `--lg-radius-pill: 9999px`, `--lg-radius-bubble: 50%` into the `:root` block alongside existing `--lg-*` tokens.
- [ ] TASK-003: Add missing duration tokens to styles.scss — `src/styles.scss` — Insert `--lg-duration-fast: 150ms`, `--lg-duration-base: 250ms`, `--lg-duration-slow: 400ms` into the `:root` block.
- [ ] TASK-004: Create shared/ui directory — `src/app/shared/ui/` — Create the empty directory that will house all new UI kit components (no code yet; prerequisite for all TASK-01x).

---

## Phase 1: Core UI Kit Components (P1 — new shared components)

- [ ] TASK-010: Create GlassCardComponent — `src/app/shared/ui/glass-card/glass-card.component.ts` — Standalone OnPush component with selector `app-glass-card`; inputs `variant: 'panel'|'module'|'card'` (default `'panel'`), `tint: 'none'|'amber'|'cyan'|'magenta'` (default `'none'`), `padding: 'none'|'sm'|'md'|'lg'` (default `'md'`); `computed()` class signal drives `[class]` binding on a `<div>` wrapping `<ng-content>`; maps to `@layer utilities` classes `.lg-panel`, `.lg-module`, `.lg-card`, `.lg-tint-*`, `.lg-pad-*` defined in `styles.scss`.
- [ ] TASK-011: Add GlassCard utility classes to styles.scss — `src/styles.scss` — Add `@layer utilities` block with `.lg-panel`, `.lg-module`, `.lg-card` (glass surface + radius), `.lg-tint-amber/cyan/magenta` (accent tint overlays), and `.lg-pad-none/sm/md/lg` (padding scale) classes.
- [ ] TASK-012: Create LiquidButtonComponent — `src/app/shared/ui/liquid-button/liquid-button.component.ts` — Standalone OnPush `app-liquid-button`; inputs `variant: 'primary'|'outline'|'ghost'` (default `'outline'`), `loading: boolean` (default `false`), `disabled: boolean` (default `false`), `size: 'sm'|'md'|'lg'` (default `'md'`); output `clicked: OutputEmitterRef<void>`; pill shape (`border-radius: var(--lg-radius-pill)`); shows a spinner `<svg>` when `loading()` is true; emits `clicked` on host click if not disabled/loading; computed class signal drives variant + size classes.
- [ ] TASK-013: Add LiquidButton utility classes to styles.scss — `src/styles.scss` — Add `.lg-btn`, `.lg-btn--primary`, `.lg-btn--outline`, `.lg-btn--ghost`, `.lg-btn--sm`, `.lg-btn--md`, `.lg-btn--lg` with pill radius, glass fills, accent glow on `--primary`, and `active:scale-95` transition.
- [ ] TASK-014: Create IconButtonComponent — `src/app/shared/ui/icon-button/icon-button.component.ts` — Standalone OnPush `app-icon-button`; inputs `size: 'sm'|'md'|'lg'` (default `'md'`), `active: boolean` (default `false`), `variant: 'default'|'accent'|'cyan'` (default `'default'`); output `clicked: OutputEmitterRef<void>`; circular shape (border-radius `var(--lg-radius-bubble)`); 44×44px default min-touch-target; `hover:bg-white/20` transition; computed class signal.
- [ ] TASK-015: Add IconButton utility classes to styles.scss — `src/styles.scss` — Add `.lg-icon-btn`, `.lg-icon-btn--sm`, `.lg-icon-btn--md`, `.lg-icon-btn--lg`, `.lg-icon-btn--accent`, `.lg-icon-btn--cyan`, `.lg-icon-btn--active` classes.
- [ ] TASK-016: Create SliderCapsuleComponent — `src/app/shared/ui/slider-capsule/slider-capsule.component.ts` — Standalone OnPush `app-slider-capsule`; `value = model<number>(50)`, `min = input<number>(0)`, `max = input<number>(100)`, `colorScheme = input<'cyan'|'gold'|'magenta'>('cyan')`; renders a thick capsule track with a fill div whose `width` is bound to a `computed()` percentage; binds a hidden `<input type="range">` for a11y that updates `value` via `(input)` event; host `display: block`.
- [ ] TASK-017: Add SliderCapsule utility classes to styles.scss — `src/styles.scss` — Add `.lg-slider-track`, `.lg-slider-fill`, `.lg-slider-fill--cyan`, `.lg-slider-fill--gold`, `.lg-slider-fill--magenta` capsule/pill track classes with gradient fills and `transition: width var(--lg-duration-base)`.
- [ ] TASK-018: Create GlassTabBarComponent — `src/app/shared/ui/glass-tab-bar/glass-tab-bar.component.ts` — Standalone OnPush `app-glass-tab-bar`; `tabs = input<{id: string, label: string, icon?: string}[]>([])`, `activeTab = model<string>('')`; renders `@for` of tab pill buttons; `computed()` returns active-tab class; emits `activeTab` update on click; ARIA `role="tablist"` with `role="tab"` and `aria-selected` on each button.
- [ ] TASK-019: Add GlassTabBar utility classes to styles.scss — `src/styles.scss` — Add `.lg-tab-bar`, `.lg-tab`, `.lg-tab--active` classes: pill switcher container with glass fill, active pill with `bg-white/15` and accent bottom border or background highlight, `transition: all var(--lg-duration-base)`.
- [ ] TASK-020: Create GlassSheetComponent — `src/app/shared/ui/glass-sheet/glass-sheet.component.ts` — Standalone OnPush `app-glass-sheet`; `isOpen = input<boolean>(false)`, `title = input<string>('')`, `maxHeight = input<string>('80vh')`; output `closed: OutputEmitterRef<void>`; renders a backdrop `<div>` and a bottom-sheet panel with a drag handle `<div>` and `<ng-content>`; `@if (isOpen())` controls visibility; `[style.max-height]="maxHeight()"` bound on panel; backdrop click emits `closed`; slide-up CSS animation on open; `role="dialog"`, `aria-modal="true"`, `aria-labelledby` wired to title.
- [ ] TASK-021: Add GlassSheet utility classes to styles.scss — `src/styles.scss` — Add `.lg-sheet-backdrop`, `.lg-sheet-panel`, `.lg-sheet-handle`, `.lg-sheet-header` classes; panel uses `fixed bottom-0 left-0 right-0`, glass fill, `border-radius: var(--lg-radius-card) var(--lg-radius-card) 0 0`, slide-up keyframe `@keyframes lg-slide-up`.
- [ ] TASK-022: Create GlassModalComponent — `src/app/shared/ui/glass-modal/glass-modal.component.ts` — Standalone OnPush `app-glass-modal`; `isOpen = input<boolean>(false)`, `title = input<string>('')`; output `closed: OutputEmitterRef<void>`; renders a full-screen backdrop and a centered `<div>` panel with `<ng-content>`; `@if (isOpen())` controls visibility; backdrop click emits `closed`; scale-in CSS animation; `role="dialog"`, `aria-modal="true"`, focus-trap via `cdkTrapFocus` or manual `tabindex` on panel.
- [ ] TASK-023: Add GlassModal utility classes to styles.scss — `src/styles.scss` — Add `.lg-modal-backdrop`, `.lg-modal-panel`, `.lg-modal-header` classes; panel: glass fill, `border-radius: var(--lg-radius-card)`, max-width 400px, `@keyframes lg-scale-in` scale 0.92→1 with opacity.
- [ ] TASK-024: Create GlassBadgeComponent — `src/app/shared/ui/glass-badge/glass-badge.component.ts` — Standalone OnPush `app-glass-badge`; `variant = input<'default'|'active'|'warning'|'error'>('default')`, `dot = input<boolean>(false)`; renders `.lg-status-badge` wrapper with optional `.lg-status-dot` span and `<ng-content>`; computed class drives variant.
- [ ] TASK-025: Add GlassBadge utility classes to styles.scss — `src/styles.scss` — Add `.lg-status-badge`, `.lg-status-badge--active`, `.lg-status-badge--warning`, `.lg-status-badge--error`, `.lg-status-dot` classes with color-coded glass pill fills.
- [ ] TASK-026: Create shared/ui barrel export — `src/app/shared/ui/index.ts` — Export all 8 new components (`GlassCardComponent`, `LiquidButtonComponent`, `IconButtonComponent`, `SliderCapsuleComponent`, `GlassTabBarComponent`, `GlassSheetComponent`, `GlassModalComponent`, `GlassBadgeComponent`) from a single index file.

---

## Phase 2: Background System Migration (P2 — fix view backgrounds)

- [x] TASK-030: Remove orb divs and fix bg in welcome.component.ts — `src/app/views/auth/welcome.component.ts` — Replace `bg-[#010208]` with `bg-transparent` on the `<section>` and delete the two absolute orb `<div>`s (the blurred teal/blue circles); background is now owned by `app-particles-background` at app level.
- [x] TASK-031: Remove orb divs and fix bg in login.component.ts — `src/app/views/auth/login.component.ts` — Replace `bg-[#010208]` with `bg-transparent` on the `<section>` and remove the inline orb `<div>` (the single `bg-emerald-600/10` blurred circle); background owned globally.
- [ ] TASK-032: Remove orb divs and fix bg in login.component.html — `src/app/views/auth/login.component.html` — Delete the three `<div class="orb orb-*">` elements that render local background orbs.
- [x] TASK-033: Remove orb divs and fix bg in invest-layout.component.ts — `src/app/views/invest/invest-layout.component.ts` — Replace `bg-[#010208]` with `bg-transparent` on the `<section>` and delete the `bg-blue-600/10` blur orb `<div>`; remove the local `.animate-float` `@keyframes` style block.
- [x] TASK-034: Remove orb divs and fix bg in social.ts — `src/app/views/social/social.ts` — Replace `bg-[#010208]` with `bg-transparent` on the `<section>` and delete the two orb `<div>`s (`bg-pink-500/10`, `bg-purple-600/10`); remove the local `.animate-float-delayed` style block.
- [x] TASK-035: Remove orb divs and fix bg in wallet.ts — `src/app/views/wallet/wallet.ts` — Replace `bg-[#010208]` with `bg-transparent` on the `<section>` and delete the two orb `<div>`s (`bg-indigo-500/10`, `bg-blue-600/10`); remove the local `.animate-float` and `.animate-float-delayed` style blocks.
- [x] TASK-036: Remove orb div and fix bg in motions.ts — `src/app/views/motions/motions.ts` — Replace `bg-[#010208]` with `bg-transparent` on the `<section>`; check for and remove any local orb divs.
- [x] TASK-037: Fix bg in game-layout.component.ts — `src/app/views/game/game-layout.component.ts` — Replace `bg-[#010208]` with `bg-transparent` on the `<section>` and delete the absolute `bg-teal-500/5` orb `<div>` and its local `@keyframes float` / `.animate-float` style block.
- [x] TASK-038: Fix bg in wallet sub-screens — `src/app/views/wallet/transaction/transaction.component.ts`, `src/app/views/wallet/transaction/payment-screen.component.ts` — Replace `bg-[#010208]` with `bg-transparent` on both `<section>` elements.
- [ ] TASK-039: Fix bg in wallet support-chat — `src/app/views/wallet/support-chat.component.ts` — Replace `bg-[#010208]` with the global surface token `bg-[var(--color-dark-background)]` (keeping it opaque for the full-screen chat view); do NOT make it transparent.
- [x] TASK-040: Fix bg in lucky-wheel.component.ts — `src/app/views/game/components/lucky-wheel/lucky-wheel.component.ts` — Replace `bg-[#010208]` with `bg-transparent` on the `<section>`.

---

## Phase 3: Component Consolidation (P3 — replace duplicated patterns)

- [ ] TASK-050: Migrate rank-tabs to GlassTabBarComponent — `src/app/views/game/components/rank/rank.component.html` & `rank.component.ts` — Replace `<app-rank-tabs>` usage with `<app-glass-tab-bar [tabs]="tabs()" [(activeTab)]="activeTab" />`; import `GlassTabBarComponent`; verify tab data shape matches `{id, label, icon?}`.
- [ ] TASK-051: Delete RankTabsComponent — `src/app/views/game/components/rank/components/rank-tabs/` — Remove `rank-tabs.component.ts`, `rank-tabs.component.html`, `rank-tabs.component.scss` after confirming no other consumer; update any barrel exports.
- [ ] TASK-052: Migrate invest layout tabs to GlassTabBarComponent — `src/app/views/invest/invest-layout.component.ts` & `.html` — Identify inline tab pill switcher in invest layout, replace with `<app-glass-tab-bar>`; import `GlassTabBarComponent`.
- [ ] TASK-053: Migrate motions tabs to GlassTabBarComponent — `src/app/views/motions/motions.ts` & `motions.html` — Replace inline tab switcher with `<app-glass-tab-bar>`; import `GlassTabBarComponent`.
- [x] TASK-054: Migrate wallet bottom sheet to GlassSheetComponent — `src/app/views/wallet/wallet.ts` & `wallet.html` — Replace the #sheet div at line ~172 (fixed bottom, liquid-glass-card rounded-t-[40px]) with <app-glass-sheet [isOpen]="sheetOpen()" [title]="sheetTitle()" (closed)="closeSheet()">; move sheet content inside <ng-content>; import GlassSheetComponent.
- [x] TASK-055: Migrate boost bottom sheets to GlassSheetComponent — `src/app/views/game/components/energy-boost/boost/boost.component.html` & `.ts` — Replace the two class="sheet" patterns (howItWorks sheet and boost-detail sheet) with <app-glass-sheet>; remove local .sheet, .sheet-backdrop, .sheet-handle, .sheet-header, .sheet-sub SCSS classes; import GlassSheetComponent.
- [ ] TASK-056: Migrate energy-boost outer sheet to GlassSheetComponent — `src/app/views/game/components/energy-boost/energy-boost.component.html` & `.ts` — Check for any additional sheet pattern in the parent energy-boost component and consolidate to `<app-glass-sheet>` if present.
- [x] TASK-057: Migrate settings modal to GlassModalComponent — `src/app/views/game/components/settings/settings.component.ts` — Replace the inline centered `<div>` modal (fixed, `-translate-x-1/2 -translate-y-1/2`, `liquid-glass-card`, `animate-[scaleIn...]`) with `<app-glass-modal [isOpen]="isOpen()" [title]="'Settings'" (closed)="close()">`; import `GlassModalComponent`; remove local `@keyframes scaleIn` style.
- [ ] TASK-058: Migrate level-menu modal to GlassModalComponent — `src/app/views/game/components/level-menu/level-menu.component.ts` & `.html` — Replace the inline centered `<div>` modal (fixed, `-translate-x-1/2 -translate-y-1/2`, `liquid-glass-card`) with `<app-glass-modal>`; import `GlassModalComponent`.
- [x] TASK-059: Migrate rank-info-modal to GlassModalComponent — `src/app/views/game/components/rank/components/rank-podium/rank-info-modal/rank-info-modal.component.ts` & `.html` — Replace local modal overlay with `<app-glass-modal>`; import `GlassModalComponent`.
- [ ] TASK-060: Migrate crypto-deposit-modal to GlassModalComponent — `src/app/views/wallet/crypto-deposit-modal.component.ts` — Replace the inline `<div>` centered modal with `<app-glass-modal>`; import `GlassModalComponent`.
- [ ] TASK-061: Migrate wallet success-overlay to GlassModalComponent — `src/app/views/wallet/transaction/components/success-overlay.component.ts` — Replace inline centered `<div>` overlay with `<app-glass-modal>`; import `GlassModalComponent`.
- [ ] TASK-062: Migrate history-modal to GlassModalComponent — `src/app/views/motions/components/history-modal.component.ts` — Replace local modal `<div>` with `<app-glass-modal>`; import `GlassModalComponent`.

---

## Phase 4: Polish + Accessibility (P4 — final touches)

- [ ] TASK-070: Add focus-visible styles to LiquidButton — `src/styles.scss` — Add `.lg-btn:focus-visible { outline: 2px solid var(--lg-accent-cyan); outline-offset: 2px; }` and equivalent for `.lg-icon-btn:focus-visible`.
- [ ] TASK-071: Add focus-visible styles to GlassTabBar tabs — `src/styles.scss` — Add `.lg-tab:focus-visible { outline: 2px solid var(--lg-accent-cyan); outline-offset: 2px; border-radius: var(--lg-radius-pill); }`.
- [ ] TASK-072: Wire ARIA live region on GlassTabBar — `src/app/shared/ui/glass-tab-bar/glass-tab-bar.component.ts` — Add `aria-live="polite"` hidden `<span>` that announces the active tab label when `activeTab` changes, matching the removed `rank-tabs` pattern.
- [ ] TASK-073: Add keyboard close (Escape) to GlassSheetComponent — `src/app/shared/ui/glass-sheet/glass-sheet.component.ts` — Add `@HostListener('document:keydown.escape')` that emits `closed` when `isOpen()` is true; set `tabindex="-1"` on panel and auto-focus on open.
- [ ] TASK-074: Add keyboard close (Escape) to GlassModalComponent — `src/app/shared/ui/glass-modal/glass-modal.component.ts` — Add `@HostListener('document:keydown.escape')` that emits `closed` when `isOpen()` is true; auto-focus the panel `div` on open for proper focus management.
- [ ] TASK-075: Add prefers-reduced-motion to styles.scss animations — `src/styles.scss` — Wrap all `@keyframes lg-slide-up`, `lg-scale-in`, and `float` with `@media (prefers-reduced-motion: no-preference)` guards; provide instant show/hide fallback for `prefers-reduced-motion: reduce`.
- [x] TASK-076: Verify bg-[#010208] fully removed from all views — *(audit task)* — Run `grep -rn "bg-\[#010208\]" src/app/views` and confirm zero remaining usages except `support-chat.component.ts` (which intentionally keeps an opaque bg).
- [ ] TASK-077: Verify duplicate `.liquid-glass-card` in app.scss is gone — *(audit task)* — Run `grep -rn "liquid-glass-card" src/app/app.scss` and confirm empty result after TASK-001.
- [ ] TASK-078: Run build and fix type errors — *(verification task)* — Run `bun run build` and resolve any TypeScript compilation errors introduced by new components or template migrations.
- [ ] TASK-079: Run unit tests and fix failures — *(verification task)* — Run `vitest run` and fix any broken test cases in `*.spec.ts` files affected by component migrations (especially `rank-tabs.component.spec.ts` if it exists, `action-buttons`, `header`, `game-layout`).
